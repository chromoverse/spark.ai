1. right now the screenshoot is not taking any screenshot - 
2. right now i said to create a weekly plan and add it to a notpead then it polluted the server dir . any data which is produced by the tool must be stored in a separate folder called .sparkai_data in the root of the project - this way we can keep the server directory clean and organized and also it will be easier for us to manage the data - we can create a function that will save the data to the .sparkai_data folder and we can call this function whenever we need to save any data from the tool - this way we can ensure that all the data is stored in a single place and it will be easier for us to access it when needed.
3. app_open tool try to use
4. add params in the comment of tool itslef so at the time of testing hover the tool class it reveals the params. for referce see server/tools/tools/system/netwerol.py file - this way it will be easier for us to understand the params and also it will be easier for us to test the tool - we can also add an example of how to use the tool in the comment so that it will be easier for us to understand how to use the tool - this way we can ensure that the tool is well documented and easy to use for anyone who wants to use it.
5. also create a .md file how to add new tool - it must show the basetool code usage a json to add in the regostry.json - a short guide to other cloud based llm to creat new tool. 

# all the data which is produced by any tool must be stored under the .sparkai_data folder in the root of the project. This is to ensure that all the data is stored in a single place and can be easily accessed by the tool. - but wait we are also storing the data in the appdata/local/sparkai folder - so we need to decide where we want to store the data - we can either store it in the .sparkai_data folder or we can store it in the appdata/local/sparkai folder - but we should not store it in both places - we need to choose one place to store the data - i think it would be better to store it in the .sparkai_data folder in the root of the project because it would be easier for the user to access the data and also it would be easier for us to manage the data - so let's go with that option and store all the data in the .sparkai_data folder in the root of the project.

## but how will we access the data from the .sparkai_data folder in the root of the project - we can create a function that will read the data from the .sparkai_data folder and return it to the tool - this function can be called get_data() and it will take the name of the tool as an argument and it will return the data for that tool - this way we can easily access the data for any tool by calling the get_data() function with the name of the tool as an argument.

## but if we do so then how the production will be done ? like is there any possible way where we can load this server using external service so it load at the time of booting so whenever the laptop get started then server must be already botted. - yes we can use a service like pm2 to run the server in the background and it will automatically start the server when the laptop is booted - we can also use a package like node-windows to create a windows service for the server so that it can run in the background and start automatically when the laptop is booted - this way we can ensure that the server is always running and ready to accept requests from the tools. 

## we will add a new command to the cli called "start-server" which will start the server in the background using pm2 or node-windows - this command will be used by the user to start the server for the first time and then it will automatically start the server on boot - we can also add a command called "stop-server" to stop the server if needed - this way we can give the user control over the server and they can start or stop it as needed.

## we will also need to add a command called "restart-server" to restart the server if needed - this command will be useful in case we need to update the server or if there are any issues with the server - this way we can ensure that the server is always running smoothly and any issues can be resolved quickly by restarting the server.
