# Spark Wake Models

Place the custom Spark wake-word ONNX files in this directory:

- `hey_spark.onnx`
- `spark.onnx`

The daemon validates both files at startup and exits with a clear error if one
is missing. These models are not generated by `install_models.py`.
